void imageInverse()
{
    int i, j, k;
    ing height, width, step, imageChannels;
    uchar *imageData;

    height = image->rows;
    width  = image->cols;
    step   = image->widthStep;
    imageChannels = image->channels;
    imageData  = (uchar *)image->data;

}
